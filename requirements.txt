# -------------------------------------------------------------------------------
# HTML-Sanitizer Requirements
# -------------------------------------------------------------------------------
# Runtime: Python standard library only (re, html, argparse, json, pathlib).
# -------------------------------------------------------------------------------

# Python 3.10+

# Tests (install with: pip install -r requirements.txt)
pytest>=8.0.0
